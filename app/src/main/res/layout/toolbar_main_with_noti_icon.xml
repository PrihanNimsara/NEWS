<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
    xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    android:background="@color/pureWhite"
    app:contentInsetEnd="0dp"
    app:contentInsetLeft="0dp"
    app:contentInsetRight="0dp"
    app:contentInsetStart="0dp">

    <RelativeLayout
        android:id="@+id/mainToolbarSela"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize">

        <TextView
            android:id="@+id/toolbarTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text=""
            android:textStyle="bold"
            android:textSize="18sp"
            android:layout_centerVertical="true"
            android:textColor="@color/colorTitle"
            android:gravity="center"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:animateLayoutChanges="true">

            <ImageView
                android:id="@+id/notificationIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center_vertical"
                android:src="@drawable/vector_drawable_notifications_none"
                android:background="@color/pureWhite"
                android:layout_marginEnd="16dp"
                android:visibility="gone"
                tools:ignore="ContentDescription"/>

            <ImageView
                android:id="@+id/profileEditIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center_vertical"
                android:src="@drawable/vector_edit"
                android:background="@color/pureWhite"
                android:visibility="gone"
                android:layout_marginEnd="18dp"
                tools:ignore="ContentDescription"/>

            <ImageView
                android:id="@+id/profileLogoutIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center_vertical"
                android:src="@drawable/vector_ic_exit_to_app_white_24dp"
                android:background="@color/pureWhite"
                android:tint="@color/profile_edit_icon"
                android:visibility="gone"
                android:layout_marginEnd="16dp"
                tools:ignore="ContentDescription"/>

        </LinearLayout>

    </RelativeLayout>
</androidx.appcompat.widget.Toolbar>